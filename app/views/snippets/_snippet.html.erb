<%= form_for @snippet do |f| %>				
	<%= f.label :filename %>			
	<%= f.text_field :filename, :size => "40"%>			
	<%= f.label :content %>
	<%= f.text_area :content, :size => "30x10" %>
	<%= f.submit %>		
<% end %>